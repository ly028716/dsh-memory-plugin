# dsh-memory-plugin - Web 查看器使用指南

## 🎯 问题解决方案

之前你提到的问题：**用户在 Web 端没有操作入口，不知道如何使用插件**。

现在我已经为你创建了一个**独立的 Web 查看器**，让用户可以直观地查看和管理记忆数据！

## 📱 如何打开查看器

### 方法 1：双击批处理文件（最简单）

```
双击: <project-root>\open-viewer.cmd
```

这会自动在浏览器中打开查看器页面。

### 方法 2：直接打开 HTML 文件

```
打开: <project-root>\viewer.html
```

在浏览器中直接打开这个 HTML 文件。

### 方法 3：拖拽到浏览器

将 `viewer.html` 文件拖拽到你的浏览器窗口中。

## 🎨 查看器功能

### 1. **数据概览**
- 显示版本号、会话数、项目数、主题数等关键统计

### 2. **用户偏好**
- 查看保存的所有偏好设置
- 包括模型、语言、Agent 等

### 3. **工具使用统计**
- 按使用频率排序的工具列表
- 清晰的数量展示

### 4. **项目列表**
- 所有管理的项目
- 包含路径、标签、最后访问时间

### 5. **最近主题**
- Top 10 最近讨论的主题
- 带时间戳

### 6. **完整数据**
- 原始 JSON 格式的全部数据
- 方便开发者调试

### 7. **操作按钮**
- 🔄 **刷新数据**：重新加载最新数据
- 💾 **导出数据**：下载 JSON 备份文件
- 🧹 **清除查看器缓存**：清除浏览器中的查看器缓存，不会删除 `.dsh-memory.json`

## 💡 使用流程

### 首次使用

1. **运行插件**：在你的代码中使用 Memory Plugin 记录一些数据
2. **打开查看器**：双击 `open-viewer.cmd`
3. **查看数据**：在浏览器中看到可视化的记忆数据

### 日常使用

1. **定期查看**：打开查看器检查记忆数据
2. **导出备份**：点击"导出数据"按钮备份
3. **清理查看器缓存**：需要时点击"清除查看器缓存"；如需删除插件源数据，请调用 `ctx.memory.clearMemory()`

## 🔧 数据位置

记忆数据默认存储在：

```
.dsh-memory.json
```

这个文件会在你使用插件的工作目录中生成。

## 📊 查看器截图说明

当你打开查看器后，会看到：

```
┌─────────────────────────────────────┐
│  🧠 Memory Plugin 查看器            │
│  查看和管理你的记忆数据              │
├─────────────────────────────────────┤
│                                     │
│  [🔄 刷新] [💾 导出] [🗑️ 清除]    │
│                                     │
│  📊 数据概览                        │
│  ┌──────┬──────┬──────┬──────┐     │
│  │版本  │会话  │项目  │主题  │     │
│  │1.0.0 │  25  │   4  │  10  │     │
│  └──────┴──────┴──────┴──────┘     │
│                                     │
│  👤 用户偏好                        │
│  {                                  │
│    "defaultModel": "qwen3.7-plus", │
│    "language": "zh-CN"             │
│  }                                  │
│                                     │
│  🛠️ 工具使用统计                   │
│  read: 156 次                       │
│  write: 89 次                       │
│  edit: 67 次                        │
│  ...                                │
│                                     │
└─────────────────────────────────────┘
```

## ⚠️ 常见问题

### Q1: 打开后显示"未找到记忆数据"

**原因**：还没有使用插件记录数据

**解决**：
1. 先在你的代码中使用 Memory Plugin
2. 运行一些操作来生成数据
3. 然后点击"刷新"按钮

### Q2: 数据没有更新

**原因**：查看器缓存了旧数据

**解决**：点击"刷新数据"按钮

### Q3: 想查看其他目录的数据

**解决**：
1. 将 `.dsh-memory.json` 文件复制到 viewer.html 所在目录
2. 或者修改 viewer.html 中的数据路径

## 🎯 与 DSH Web 端的集成（未来计划）

目前这是一个独立的查看器。未来可以考虑：

1. **DSH 插件面板**：在 DSH Web 端添加 Memory Plugin 配置面板
2. **实时同步**：与 DSH 的设置系统整合
3. **可视化图表**：更丰富的数据可视化
4. **管理界面**：直接在 Web 端管理记忆数据

## 📝 总结

现在你有两种方式查看记忆数据：

1. **HTML 报告**（静态）：`memory-report.html` - 精美的固定报告
2. **Web 查看器**（动态）：`viewer.html` - 可交互的数据查看器

**推荐使用 Web 查看器**，因为它：
- ✅ 可以刷新获取最新数据
- ✅ 可以导出备份
- ✅ 可以清除查看器缓存
- ✅ 操作简单直观

---

**立即体验**：双击 `open-viewer.cmd` 开始使用！🚀
