# 真实浏览器 E2E 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 使用 Playwright Chromium 对 `viewer.html` 执行真实浏览器端到端测试，覆盖主数据加载、缓存清除后的重新加载和浏览器下载导出。

**架构：** Playwright Test 通过 `webServer` 启动一个仅暴露 viewer 页面和固定 fixture 的 Node HTTP server。每个 spec 使用隔离 browser context，在真实 Chromium 中断言可见 UI、localStorage、HTTP 源数据和下载文件；CI 安装 Chromium 并上传失败产物。

**技术栈：** Node.js 20/22、CommonJS、`@playwright/test`、Node 内置 `http`、现有静态 `viewer.html`。

---

## 文件清单

- 创建：`playwright.config.js` — Chromium 项目、webServer、超时、报告和失败产物。
- 创建：`test/browser-server.js` — 只读 viewer/fixture HTTP server。
- 创建：`test/fixtures/browser-memory.json` — 稳定的浏览器 E2E 记忆数据。
- 创建：`test/e2e/viewer.spec.js` — 三条真实浏览器用户流程。
- 修改：`package.json` — Playwright 依赖、`test:browser-e2e` 脚本。
- 修改：`package-lock.json` — 锁定 Playwright 依赖树。
- 修改：`.gitignore` — 忽略 Playwright 报告和测试产物。
- 修改：`.github/workflows/ci.yml` — 安装 Chromium、运行浏览器 E2E、上传报告。
- 修改：`.github/workflows/release.yml` — 发布前安装 Chromium 并运行浏览器 E2E。
- 修改：`README.md`、`README.en.md`、`USAGE.md` — 浏览器安装和运行说明。
- 创建：`test/browser-server.test.js` — server 路由和安全边界测试。

### 任务 1：增加 Playwright 配置和依赖

**文件：**
- 修改：`package.json`
- 修改：`package-lock.json`
- 创建：`playwright.config.js`
- 修改：`.gitignore`

- [ ] **步骤 1：编写配置契约测试**

在 `test/release-ci.test.js` 增加：

```js
test('declares the real browser E2E command and Playwright config', () => {
  const packageJson = JSON.parse(fs.readFileSync(path.join(__dirname, '..', 'package.json'), 'utf8'));
  expect(packageJson.scripts['test:browser-e2e']).toBe('playwright test --config=playwright.config.js');
  expect(packageJson.devDependencies['@playwright/test']).toBeDefined();
  expect(fs.existsSync(path.join(__dirname, '..', 'playwright.config.js'))).toBe(true);
});
```

- [ ] **步骤 2：运行契约测试确认失败**

运行：`npx jest test/release-ci.test.js --runInBand`

预期：FAIL，缺少 `test:browser-e2e` 和 `@playwright/test`。

- [ ] **步骤 3：安装 Playwright 并实现配置**

运行：`npm install --save-dev @playwright/test`

在 `playwright.config.js` 中实现：

```js
const { defineConfig, devices } = require('@playwright/test');

module.exports = defineConfig({
  testDir: './test/e2e',
  fullyParallel: true,
  forbidOnly: Boolean(process.env.CI),
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: process.env.CI
    ? [['line'], ['html', { outputFolder: 'playwright-report', open: 'never' }], ['junit', { outputFile: 'test-results/browser-e2e.xml' }]]
    : [['list'], ['html', { outputFolder: 'playwright-report', open: 'never' }]],
  use: {
    ...devices['Desktop Chrome'],
    baseURL: 'http://127.0.0.1:4173',
    headless: true,
    screenshot: 'only-on-failure',
    trace: 'retain-on-failure',
    video: 'retain-on-failure',
    actionTimeout: 10000,
    navigationTimeout: 30000
  },
  webServer: {
    command: 'node test/browser-server.js',
    url: 'http://127.0.0.1:4173/viewer.html',
    reuseExistingServer: false,
    timeout: 30000
  }
});
```

在 `package.json` 增加：

```json
"test:browser-e2e": "playwright test --config=playwright.config.js"
```

并将 `playwright-report/`、`test-results/`、`test-artifacts/` 加入 `.gitignore`。

- [ ] **步骤 4：运行配置契约测试确认通过**

运行：`npx jest test/release-ci.test.js --runInBand`

预期：全部 release/config 契约测试 PASS。

- [ ] **步骤 5：提交 Playwright 基础设施**

```bash
git add package.json package-lock.json playwright.config.js .gitignore test/release-ci.test.js
git commit -m "test: configure real browser e2e runner"
```

### 任务 2：实现隔离 HTTP fixture server

**文件：**
- 创建：`test/browser-server.js`
- 创建：`test/fixtures/browser-memory.json`
- 创建：`test/browser-server.test.js`

- [ ] **步骤 1：编写 server 路由失败测试**

```js
test('serves only viewer and memory fixture routes', async () => {
  const server = await startServerForTest();
  await expect(fetch(`${server.url}/viewer.html`)).resolves.toMatchObject({ status: 200 });
  await expect(fetch(`${server.url}/.dsh-memory.json`)).resolves.toMatchObject({ status: 200 });
  await expect(fetch(`${server.url}/package.json`)).resolves.toMatchObject({ status: 404 });
  await server.close();
});

test('serves valid fixture JSON with a stable content type', async () => {
  const server = await startServerForTest();
  const response = await fetch(`${server.url}/.dsh-memory.json`);
  expect(response.headers.get('content-type')).toContain('application/json');
  expect((await response.json()).userPreferences.defaultModel).toBe('browser-e2e-model');
  await server.close();
});
```

- [ ] **步骤 2：运行 server 测试确认失败**

运行：`npx jest test/browser-server.test.js --runInBand`

预期：FAIL，server 模块和 fixture 尚不存在。

- [ ] **步骤 3：创建固定 fixture**

`test/fixtures/browser-memory.json` 至少包含以下可观察数据：

```json
{
  "version": "1.1.0",
  "metadata": { "createdAt": "2026-01-01T00:00:00.000Z", "totalSessions": 7, "lastSessionDate": "2026-01-07T00:00:00.000Z" },
  "userPreferences": { "defaultModel": "browser-e2e-model", "language": "zh-CN" },
  "projectContext": { "activeProjects": [{ "name": "browser-fixture-project", "path": "/tmp/browser-fixture-project", "tags": ["e2e"], "lastAccessed": "2026-01-07T00:00:00.000Z" }] },
  "sessionHistory": { "recentTopics": [{ "content": "browser e2e topic", "timestamp": "2026-01-07T00:00:00.000Z" }], "frequentTasks": [], "toolUsageStats": { "browser-tool": 4 } }
}
```

- [ ] **步骤 4：实现安全 server**

`test/browser-server.js` 使用 Node `http.createServer`，固定监听 `127.0.0.1:4173`（可由 `PLAYWRIGHT_PORT` 覆盖），只响应 `/`、`/viewer.html` 和 `/.dsh-memory.json`；页面内容从仓库根读取，fixture 内容从 `test/fixtures/browser-memory.json`读取；其他路径 404。启动后输出 `BROWSER_E2E_SERVER_READY`，SIGINT/SIGTERM 关闭 server。

导出 `createBrowserServer({ port, rootDir, fixturePath })` 和 `startFromCli()`，让 Jest 可以在随机端口测试路由而不启动子进程。

- [ ] **步骤 5：运行 server 测试确认通过**

运行：`npx jest test/browser-server.test.js --runInBand`

预期：server 路由和 MIME 类型测试 PASS。

- [ ] **步骤 6：提交 fixture server**

```bash
git add test/browser-server.js test/browser-server.test.js test/fixtures/browser-memory.json
git commit -m "test: add isolated viewer fixture server"
```

### 任务 3：添加真实 Chromium 查看器流程

**文件：**
- 创建：`test/e2e/viewer.spec.js`

- [ ] **步骤 1：编写真实浏览器 spec**

使用 `@playwright/test`，每个测试用独立 context；页面对象辅助函数只通过可观察 locator 工作：

```js
const { test, expect } = require('@playwright/test');

async function openViewer(page) {
  await page.goto('/viewer.html');
  await expect(page.locator('#loading')).toBeHidden();
  await expect(page.locator('#content')).toBeVisible();
}

test('loads fixture data from the HTTP source and renders the viewer', async ({ page }) => {
  await openViewer(page);
  await expect(page.locator('.stat-card').filter({ hasText: '会话数' }).locator('.value')).toHaveText('7');
  await expect(page.locator('.stat-card').filter({ hasText: '项目数' }).locator('.value')).toHaveText('1');
  await expect(page.locator('#dataContent')).toContainText('browser-e2e-model');
  await expect(page.locator('#dataContent')).toContainText('browser-fixture-project');
  await expect(page.locator('#dataContent')).toContainText('browser e2e topic');
  await expect(page.evaluate(() => localStorage.getItem('memory-plugin-data'))).resolves.toContain('browser-e2e-model');
});

test('clears viewer cache without deleting source data', async ({ page }) => {
  await openViewer(page);
  const dialogs = [];
  page.on('dialog', async (dialog) => {
    dialogs.push(dialog.message());
    await dialog.accept();
  });
  await page.getByRole('button', { name: '清除查看器缓存' }).click();
  await expect(page.locator('#content')).toBeHidden();
  expect(await page.evaluate(() => localStorage.getItem('memory-plugin-data'))).toBeNull();
  expect(dialogs.some((message) => message.includes('原始记忆文件不会被修改'))).toBe(true);
  await page.reload();
  await expect(page.locator('#content')).toBeVisible();
  expect(await page.evaluate(() => localStorage.getItem('memory-plugin-data'))).toContain('browser-e2e-model');
  expect((await page.request.get('/.dsh-memory.json')).status()).toBe(200);
});

test('downloads the current memory as JSON', async ({ page }) => {
  await openViewer(page);
  const dialogs = [];
  page.on('dialog', async (dialog) => {
    dialogs.push(dialog.message());
    await dialog.accept();
  });
  const downloadPromise = page.waitForEvent('download');
  await page.getByRole('button', { name: '导出数据' }).click();
  const download = await downloadPromise;
  expect(download.suggestedFilename()).toMatch(/^memory-backup-\d{4}-\d{2}-\d{2}\.json$/);
  expect(JSON.parse(await require('fs').promises.readFile(await download.path(), 'utf8')).version).toBe('1.1.0');
  expect(dialogs.some((message) => message.includes('数据已导出'))).toBe(true);
});
```

- [ ] **步骤 2：安装 Chromium 并运行真实浏览器测试**

运行：`npx playwright install chromium`

运行：`npm run test:browser-e2e`

预期：3 个 Chromium 测试 PASS，并生成 `playwright-report/`；不允许用 `--headed` 或 jsdom 替代真实浏览器验证。

- [ ] **步骤 3：验证失败产物配置**

运行：`npm run test:browser-e2e -- --grep "does not exist"`

预期：命令返回非零，并在 `test-results/` 或 `playwright-report/` 生成失败诊断产物；随后删除该临时失败产物，不提交到 Git。

- [ ] **步骤 4：提交 Chromium spec**

```bash
git add test/e2e/viewer.spec.js
git commit -m "test: cover viewer flows in real chromium"
```

### 任务 4：接入 CI、文档和最终验证

**文件：**
- 修改：`.github/workflows/ci.yml`
- 修改：`.github/workflows/release.yml`
- 修改：`README.md`
- 修改：`README.en.md`
- 修改：`USAGE.md`

- [ ] **步骤 1：增加 CI 浏览器安装和 E2E 步骤**

在 CI 和 release 的 `npm ci` 后增加：

```yaml
- name: Install Chromium for browser E2E
  run: npx playwright install --with-deps chromium

- name: Real browser E2E
  run: npm run test:browser-e2e

- name: Upload browser E2E report
  if: always()
  uses: actions/upload-artifact@v4
  with:
    name: browser-e2e-${{ matrix.node-version || 'release' }}
    path: |
      playwright-report/
      test-results/
    if-no-files-found: ignore
```

release workflow 没有 matrix 时使用固定 artifact 名称，不能引用不存在的 `matrix.node-version`。

- [ ] **步骤 2：更新中英文运行文档**

说明：

```bash
npx playwright install chromium
npm run test:browser-e2e
```

注明浏览器 E2E 与现有 `npm test`、`npm run test:dsh-e2e` 相互独立，失败时检查 `playwright-report/` 和 `test-results/`。

- [ ] **步骤 3：运行完整验证**

运行：`npm run check`

运行：`npm test -- --runInBand`

运行：`npm run test:browser-e2e`

运行：`npm run test:package`

运行：`npm run test:integration`

运行：`git diff --check`

预期：语法检查、现有 194 个 Jest 测试、3 个真实 Chromium 测试、打包检查和集成测试全部通过；工作区无未追踪报告产物。

- [ ] **步骤 4：提交 CI 和文档**

```bash
git add .github/workflows/ci.yml .github/workflows/release.yml README.md README.en.md USAGE.md
git commit -m "ci: run real browser e2e for viewer"
```
